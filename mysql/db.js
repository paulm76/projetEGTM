var mysql = require('mysql');

export const doCreateUser = (id, lastName, firstName, email, street, zipCode, city) =>
  //mysql connnection

export const doCreateTeam = (admin, name) =>
  //mysql connnection

export const onGetTeam = (admin, name) => 
  //mysql connnection